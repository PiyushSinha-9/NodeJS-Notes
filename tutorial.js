const sum = (num1, num2) => num1+num2;
const sub = (num1, num2) => num1-num2;
const mul = (num1, num2) => num1*num2;

// // Invidually
// module.exports.sum = sum;
// module.exports.sub = sub;

module.exports = {sum: sum, sub: sub, mul: mul};